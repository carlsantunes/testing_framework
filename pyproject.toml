[project]
name = "testing-utils"
version = "0.1.0"
description = "Utilities for testing databricks tables"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "beautifulsoup4>=4.12",
  "pandas",
  "pytz",
  "requests"
]
